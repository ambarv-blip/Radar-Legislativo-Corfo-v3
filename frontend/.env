# Vacío a propósito: el frontend usa rutas relativas (/api) que Vite reenvía
# server-side a http://127.0.0.1:8000 (ver server.proxy en vite.config.js).
# Solo define VITE_API_URL si el backend corre en otro host (ej. producción).
VITE_API_URL=
